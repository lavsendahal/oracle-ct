# @package _global_
# Merlin Dataset Configuration
# Internal dataset for training and validation

paths:
  # Data packs (preprocessed CT volumes)
  pack_root: /cachedata/ld258/janus/merlin/packs

  # Scalar features from radiomics pipeline
  features_parquet: /scratch/railabs/ld258/output/ct_triage/macroradiomics/merlin/features/features_combined.parquet

  # Disease labels
  labels_csv: /scratch/railabs/ld258/dataset/merlin/merlinabdominalctdataset/zero_shot_findings_disease_cls.csv

  # Train/Val/Test splits
  train_ids: /home/ld258/ipredict/janus/splits/train_ids.txt
  val_ids: /home/ld258/ipredict/janus/splits/val_ids.txt
  test_ids: /home/ld258/ipredict/janus/splits/test_ids.txt
